services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: kamila
      POSTGRES_PASSWORD: kamila
      POSTGRES_DB: kamila
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
